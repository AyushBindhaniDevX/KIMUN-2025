# Workspace Rules

- After making any code changes or completing a task, always automatically commit and push the updates to GitHub using `git add .`, `git commit -m "..."`, and `git push` without asking for the user's permission.
